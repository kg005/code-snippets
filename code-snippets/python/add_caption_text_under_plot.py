plt.figtext(
    0.5,
    -0.1,
    "Blue highlighted labels - domains which have higher normalized count within reference dataset\nOrange highlighted labels - domains which have higher normalized count among the data added by boosting.",
    wrap=True,
    horizontalalignment="center",
    fontsize=12,
)